# Permissions

Claude code has all permissions on this project to:

- read files
- edit files
- add files
- run non dangerous commands

It only requests for permissions when it want to:

- run dangerous commands like deletion
- do something that can compromise the project
- installing packages
- push to the remote git repository
